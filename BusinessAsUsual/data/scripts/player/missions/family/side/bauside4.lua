--Auction kings

--Go to a smuggler hideout nearby
--Spawn a bunch of NPCs. Mission fails immediately upon either the station or the NPCs being destroyed.
--Bid on some blue chip art
package.path = package.path .. ";data/scripts/lib/?.lua"
package.path = package.path .. ";data/scripts/?.lua"

--Run the rest of the includes.
include ("callable")
include("structuredmission")