--Going to the mat

--Warp in to kill some pirates and drop a beacon blaming another pirate faction.
--Repeat in another sector
--Go to randomly chosen nearby sector - kill all the pirates that spawn (they will spawn from both factions)
--The idea here is to let the pirates kill each other first, but if you mop them up yourself that's fine.
package.path = package.path .. ";data/scripts/lib/?.lua"
package.path = package.path .. ";data/scripts/?.lua"

--Run the rest of the includes.
include ("callable")
include("structuredmission")